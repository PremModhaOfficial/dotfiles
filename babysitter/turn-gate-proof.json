{
  "proof": "<promise>COMPLETION_PROOF</promise>\n\nTurn-end gate hook implemented and verified in /home/prem-modha/.jcode/source/jcode:\n\n1. Contract satisfied: turn_end_gate hook stdout JSON {\"decision\":\"block\",\"followup_message\":\"<text>\"} => followup queued and injected as next turn instruction (loop in process_message_streaming_mpsc, bounded MAX_FOLLOWUP_TURNS=1000).\n2. Observer behavior preserved: no gate configured => pure observer semantics (turn_end observer unchanged, herdr-agent-state.sh wiring untouched). Exit 0 without block JSON / malformed JSON / exit 2 / timeout / spawn failure => Continue, no injection.\n3. Build: cargo check -p jcode-base -p jcode-config-types -p jcode-app-core passes clean (0 warnings).\n4. Tests: cargo test -p jcode-base --lib hooks 11/11 pass (3 new parse_turn_end_followup tests). cargo test -p jcode-app-core --lib 1058/1061: the 3 failures (tool::bash stdin forwarding x2, tool::todo schema) are pre-existing environment flakes confirmed on this tree (bash stdin test times out waiting for stdin request), unrelated to the hook change.\n5. Config: HooksConfig.turn_end_gate + turn_end_gate_timeout_ms with JCODE_HOOK_TURN_END_GATE / JCODE_HOOK_TURN_END_GATE_TIMEOUT_MS env overrides, additive defaults preserved."
}
